package com.example.desafio.api.models.repositorys;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelCarRepository extends JpaRepository<ModelCarRepository, Long> {
}
